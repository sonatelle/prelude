# Load the flake development shell when entering this directory.
# Requires direnv; nix-direnv is recommended for caching.
#
# When using languages.go.version = "mod", reload when go.mod changes
# (nix-direnv does not watch go.mod by default).
watch_file go.mod
use flake
